import CartPageClient from "@/components/common/CartPageClient";

export const metadata = {
  title: "Shopping Cart | Cartify",
  description:
    "Review all the products in your cart and proceed to checkout safely.",
};

export default function CartPage() {
  return <CartPageClient />;
}
